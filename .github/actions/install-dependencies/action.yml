runs:
  using: "composite"
  steps:
    - name: Update
      run: sudo apt update
      shell: bash

    - name: Install c++ libs
      run: |
        sudo apt install libxerces-c-dev -y
        sudo apt install libboost-program-options-dev -y
      shell: bash
